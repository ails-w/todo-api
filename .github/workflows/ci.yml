name: CI

on:
  workflow_dispatch:

jobs:
  bootstrap:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Current state
        run: |
          echo "This repository is in documentation/bootstrap mode."
          echo "The ASP.NET Core solution will be added in a later phase."

