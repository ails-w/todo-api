# Progreso — 2026-06-11

## Sesión 4 — Fase 3: POST /tasks (crear tarea) completa

### Qué se trabajó

- se creó `CreateTaskRequest` (DTO de entrada con `[Required]` y `[StringLength]`)
- se agregó `CreateAsync` a `ITaskRepository` y se implementó en `InMemoryTaskRepository` (asigna Guid automático)
- se agregó `CreateAsync` a `ITaskService` y `TaskService` (mapeo CreateTaskRequest → TaskItem → TaskResponse)
- se agregó `POST /api/tasks` al controller (201 Created + Location header con `CreatedAtAction`)
- se reestructuró el checklist para que los tests aparezcan primero (flujo TDD)

### Tests creados (TDD — escritos antes de la implementación)

- `Post_Task_Valid_Returns_201_Created_With_Location` → verifica 201, Location header, body, persistencia
- `Post_Task_Invalid_Returns_400_BadRequest` → verifica 400 con Title vacío

### Código creado

- `src/TodoApi.Api/Features/Tasks/Dtos/CreateTaskRequest.cs`

### Archivos modificados

- `src/TodoApi.Api/Features/Tasks/Contracts/ITaskRepository.cs` — agregado `CreateAsync`
- `src/TodoApi.Api/Infrastructure/InMemory/InMemoryTaskRepository.cs` — implementado `CreateAsync`
- `src/TodoApi.Api/Features/Tasks/Services/ITaskService.cs` — agregado `CreateAsync`
- `src/TodoApi.Api/Features/Tasks/Services/TaskService.cs` — implementado `CreateAsync`
- `src/TodoApi.Api/Controllers/TasksController.cs` — agregado `POST /api/tasks`
- `tests/TodoApi.Tests/TasksControllerTests.cs` — reorganizado (POST primero, GET después)
- `docs/checklists/project-phases.md` — Fase 3 reestructurada con tests primero
- `docs/checklists/project-phases.md` — Fases 4-8 reestructuradas con tests primero

### Conceptos aprendidos

1. **DTO de entrada** — `CreateTaskRequest` con propiedades inmutables (`init`)
2. **Data Annotations** — `[Required]`, `[StringLength]` para validación declarativa
3. **Model binding + `[ApiController]`** — validación automática → 400 Bad Request
4. **201 Created + Location header** — semántica HTTP para creación de recursos
5. **`CreatedAtAction`** — genera 201 + Location + body en un helper
6. **TDD con tests de integración** — escribir el test antes que el código, RED → GREEN

### Problemas encontrados

- `CreatedAtAction` con `WebApplicationFactory` genera URL absoluta (`http://localhost/api/Tasks/{id}`), no relativa. Se ajustó el test para usar `Assert.Contains` en vez de regex absoluta.

### Qué quedó pendiente

- Fase 4 (PUT), Fase 5 (DELETE), Fase 6 (Swagger), Fase 7 (Error handling), Fase 8 (CI/CD)

### Siguiente paso

Arrancar Fase 4 (PUT /tasks/{id}).
